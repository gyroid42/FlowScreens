namespace FlowStates.FlowMessageUnion
{
    public enum FlowMessageType : byte
    {
        MENU_NAVIGATION,
        CONFIRM_POPUP,
        ALPHA_SELECT,
        SET_ANIMATION_SPEED,
        TRY_DO_ACTION,
        OPEN_WINDOW
    }
}